package com.noxcrew.interfaces.utilities

/** The segment of the inventory being drawn to. */
public enum class InventorySegment {
    /** The custom container. */
    CONTAINER,

    /** The player inventory. */
    PLAYER,
}
