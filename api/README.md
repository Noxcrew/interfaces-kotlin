# Interfaces API

A small pure Java API featuring any parts of interfaces's API that do not rely on Bukkit. This is split out so interfaces concepts such as
triggers can be used as components in other non-Bukkit APIs used primarly in Bukkit plugins.